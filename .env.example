# 数据库连接字符串 (PostgreSQL)
DATABASE_URL=postgresql://用户名:密码@主机:端口/数据库名

# Session 加密密钥（至少 32 位随机字符串，生产环境务必更换）
SESSION_PASSWORD=请替换为至少32位的随机字符串

# Resend API Key（用于发送邮箱验证码）
RESEND_API_KEY=re_xxxxxxxxxxxx

# 运行环境（production 时 cookie 仅 HTTPS）
NODE_ENV=development